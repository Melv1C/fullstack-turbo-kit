# Admin Dashboard

A React + Vite admin dashboard application built with TanStack Router and React Query.

## Features

- ⚡ Vite for fast development
- 🎨 Tailwind CSS for styling
- 🧭 TanStack Router for routing
- 📊 React Query for data fetching
- 🔒 Zod for environment validation
- 📦 Shared UI components from `@repo/ui`
- 🌐 API client from `@repo/api-client`

## Getting Started

```bash
npm install
npm run dev
```

The admin app will be available at `http://localhost:5174` by default.

## Build

```bash
npm run build
```

## Lint

```bash
npm run lint
```

## Type Check

```bash
npm run check-types
```
