# @repo/ui

Shared React component library.

## Components

All components are built with TypeScript and can be imported by apps in the monorepo.

## Usage

```tsx
import { Button } from '@repo/ui';

function App() {
  return <Button>Click me</Button>;
}
```

## Adding Components

Create new components in `src/` and export them from `src/index.ts`.
